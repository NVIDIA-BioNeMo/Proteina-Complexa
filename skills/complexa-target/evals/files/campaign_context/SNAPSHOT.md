# Campaign documentation snapshot

Read-only project documentation and source configuration for an explanation task.
This is a partial snapshot, not an installed runtime or a runnable checkout.
Model weights, target structures, target registries, and site-specific SLURM
account/partition settings are not supplied.

The README excerpts describe the CLI and pipeline stages. The two configuration
files are unmodified source copies. Use paths relative to this snapshot when
reading them, and ordinary repository-relative paths in future command examples.
Source revision and hashes are recorded in `source_manifest.json`.
