{
  "skill_name": "complexa-sweep",
  "evals": [
    {
      "id": "complexa-sweep-001",
      "prompt": "First inspect the actual resources allocated to this task; do not assume the host is CPU-only. Use the skill preflight if available, or equivalent bounded local probes in the baseline. Check GPU visibility with nvidia-smi and, if necessary, device allocation or an already installed CUDA runtime. A failed nvidia-smi call or preflight gpu.available=false alone does not prove GPU absence: distinguish a CPU allocation from a broken GPU probe using the available device/allocation evidence. Do not install a framework just to probe hardware. Save the probe output under /workspace/output.\n\nChoose exactly one branch:\n- GPU available: carry out the original request below on the prepared Complexa installation. Check the required runtime, GPU memory, checkpoints, refolder/tool dependencies, and target inputs first. /workspace/input/project contains partial configuration review fixtures, not a runnable installation. If GPU hardware is present but prerequisites are missing, the driver/probe fails, or execution fails/times out, report the exact blocker or failure; do not relabel the environment as CPU-only or substitute the CPU fallback for a successful GPU result. Ask for genuinely missing target/motif inputs instead of inventing them. Do not spend the trial cloning repositories, installing the ML stack, compiling extensions, or downloading weights; the GPU runner must be provisioned before this task.\n- No GPU available: complete only the CPU fallback below, using the supplied files and any installed skill references. Do not run inference, submit GPU jobs, install packages, clone repositories, or download model weights. Synthetic fixture values are test data, never measured design results.\n- Hardware availability cannot be determined: report the probe failure as a blocker, without claiming either a CPU completion or successful GPU execution.\n\nWrite /workspace/output/execution_mode.json containing selected_mode (gpu, cpu, or undetermined), task_status (completed, blocked, failed, or awaiting_confirmation), gpu_execution (completed, not_run, or failed), reason, and evidence (paths to the actual probe logs and, when applicable, execution logs/results). Keep the selected mode fixed if a GPU run fails. Also state the selected mode, actual work completed, output paths, and remaining blockers in the final answer. GPU success requires real completed-run evidence. CPU completion validates the fallback only and leaves the GPU test unexercised. Never fabricate results or expose secrets. For the GPU branch, retain the original sweep workflow, including cost estimation and any required launch confirmation. Execute once launch is authorized. If confirmation is still required, finish the concrete sweep setup, report awaiting_confirmation and gpu_execution=not_run, and do not claim a measured GPU result. Before the final answer, read back /workspace/output/execution_mode.json and show it together with the hardware-probe result in tool output. Reopen the written task artifacts and show their relevant contents or computed results; for large GPU outputs, show a bounded summary of actual run logs, counts, and metrics with their file paths.\n\nOriginal request (GPU branch):\nRun a complexa sweep over beam_width [4, 8, 16] and nsteps [100, 200, 400] for the PDL1 target using the search_binder_pipeline.\n\nCPU fallback (only when no GPU is available):\nUse the source config snippets in /workspace/input/project to look up parameter keys. Prepare an offline Complexa sweep over beam_width [4, 8, 16] and nsteps [100, 200, 400] for 02_PDL1 using search_binder_pipeline. Write sweeper.yaml, a CSV enumerating all parameter combinations, and launch_plan.md with the future config-generator command. For budgeting only, assume 20 minutes on one GPU per combination; clearly label that assumption. Write the requested fallback artifacts under /workspace/output.",
      "expected_output": "Hardware-dependent outcome. GPU branch: The agent used complexa-sweep to author or select a sweeper YAML with beam_width and nsteps axes, computed the cartesian product (9 configs), estimated GPU cost, and requested user confirmation before proceeding with launch. CPU branch, only when no GPU is available: The agent creates the nine-row parameter grid, correct dot-path sweeper, and a 3 GPU-hour estimate with a future launch plan. A sweep awaiting required cost confirmation may complete the original preparation criteria but does not count as executed GPU validation; blocked setup or a failed launch is not a successful GPU result. Hardware selection and the completed work are documented in /workspace/output/execution_mode.json and the final answer, with the saved manifest and relevant artifact contents read back into tool output.",
      "assertions": [
        "The agent saved actual bounded hardware-probe evidence and /workspace/output/execution_mode.json with selected_mode, task_status, gpu_execution, reason, and evidence paths, then read back the saved manifest and probe result in tool output. The mode agrees with the observed allocation; a failed probe or gpu.available=false alone does not establish GPU absence. The manifest alone is not evidence that a GPU run occurred.",
        "GPU presence selects the original GPU branch, and confirmed GPU absence selects the CPU fallback. Missing dependencies/weights/target inputs, insufficient GPU memory, broken probes/drivers, or GPU execution failures are reported as blocked/failed rather than a CPU success. Undetermined hardware is blocked. No installation/download loop is used to hide these blockers.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent performed a preflight check to check GPU availability. CPU, with confirmed GPU absence: The sweeper uses generation.search.beam_search.beam_width=[4,8,16] and generation.args.nsteps=[100,200,400]. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent computed the cost estimate (9 configs \u00d7 1 target = 9 runs) and presented it to the user for confirmation. CPU, with confirmed GPU absence: The agent writes a nine-row combinations CSV with each Cartesian-product combination exactly once. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent authored or identified a sweeper YAML in configs/sweeps/ with the specified beam_width and nsteps values. CPU, with confirmed GPU absence: The cost estimate is 9 runs times 20 minutes = 180 GPU-minutes = 3 GPU-hours, explicitly based on the supplied estimate. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent explained the launch mechanism via generate_inference_configs.py --sweeper or SLURM launcher. CPU, with confirmed GPU absence: The launch plan passes --sweeper to script_utils/generate_inference_configs.py and explains subsequent complexa design execution; it does not pass --sweeper directly to complexa design. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent reports the sweep artifacts and required cost confirmation, or the actual outputs of an authorized completed sweep. CPU, with confirmed GPU absence: The artifacts preserve beam-search selection and target 02_PDL1 and record that launch needs a prepared GPU environment and confirmation of cost. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "The final answer and execution_mode.json distinguish a completed CPU fallback, a real GPU execution, a blocked/failed task, and a sweep awaiting confirmation. Synthetic CSV data is explicitly labeled. CPU completion is not presented as validation of GPU inference or design quality, and no secrets or fabricated scientific results are reported."
      ],
      "expected_skill": "complexa-sweep",
      "expected_script": null,
      "files": [
        "files/project"
      ]
    },
    {
      "id": "complexa-sweep-002",
      "prompt": "First inspect the actual resources allocated to this task; do not assume the host is CPU-only. Use the skill preflight if available, or equivalent bounded local probes in the baseline. Check GPU visibility with nvidia-smi and, if necessary, device allocation or an already installed CUDA runtime. A failed nvidia-smi call or preflight gpu.available=false alone does not prove GPU absence: distinguish a CPU allocation from a broken GPU probe using the available device/allocation evidence. Do not install a framework just to probe hardware. Save the probe output under /workspace/output.\n\nChoose exactly one branch:\n- GPU available: carry out the original request below on the prepared Complexa installation. Check the required runtime, GPU memory, checkpoints, refolder/tool dependencies, and target inputs first. /workspace/input/project contains partial configuration review fixtures, not a runnable installation. If GPU hardware is present but prerequisites are missing, the driver/probe fails, or execution fails/times out, report the exact blocker or failure; do not relabel the environment as CPU-only or substitute the CPU fallback for a successful GPU result. Ask for genuinely missing target/motif inputs instead of inventing them. Do not spend the trial cloning repositories, installing the ML stack, compiling extensions, or downloading weights; the GPU runner must be provisioned before this task.\n- No GPU available: complete only the CPU fallback below, using the supplied files and any installed skill references. Do not run inference, submit GPU jobs, install packages, clone repositories, or download model weights. Synthetic fixture values are test data, never measured design results.\n- Hardware availability cannot be determined: report the probe failure as a blocker, without claiming either a CPU completion or successful GPU execution.\n\nWrite /workspace/output/execution_mode.json containing selected_mode (gpu, cpu, or undetermined), task_status (completed, blocked, failed, or awaiting_confirmation), gpu_execution (completed, not_run, or failed), reason, and evidence (paths to the actual probe logs and, when applicable, execution logs/results). Keep the selected mode fixed if a GPU run fails. Also state the selected mode, actual work completed, output paths, and remaining blockers in the final answer. GPU success requires real completed-run evidence. CPU completion validates the fallback only and leaves the GPU test unexercised. Never fabricate results or expose secrets. For the GPU branch, retain the original sweep workflow, including cost estimation and any required launch confirmation. Execute once launch is authorized. If confirmation is still required, finish the concrete sweep setup, report awaiting_confirmation and gpu_execution=not_run, and do not claim a measured GPU result. Before the final answer, read back /workspace/output/execution_mode.json and show it together with the hardware-probe result in tool output. Reopen the written task artifacts and show their relevant contents or computed results; for large GPU outputs, show a bounded summary of actual run logs, counts, and metrics with their file paths.\n\nOriginal request (GPU branch):\nI want to find the optimal generation parameters for my binder design. Can you compare different temperatures (0.5, 1.0, 1.5) and search replicas (2, 4, 8) to see which combination gives the best success rate on the DerF21 target?\n\nCPU fallback (only when no GPU is available):\nUse the source config snippets in /workspace/input/project to look up parameter keys. Compare an already completed example parameter grid for 22_DerF21 from /workspace/input/sweep_results.csv. This CSV is a synthetic test fixture, not experimental evidence. It covers backbone noise temperatures [0.5, 1.0, 1.5] and best-of-N replicas [2, 4, 8]. Write the corresponding sweeper.yaml and sweep_summary.csv ranked by success rate descending, then mean_i_pae ascending. Report the best row in the fixture. Write the requested fallback artifacts under /workspace/output.",
      "expected_output": "Hardware-dependent outcome. GPU branch: The agent recognized this as a parameter sweep request, set up a cartesian-product scan over bb_ca_temperature and search_replicas, estimated GPU hours for the 9-config sweep, and prepared to generate a ranked summary CSV identifying the best configuration by success rate. CPU branch, only when no GPU is available: The agent maps both axes to real Complexa keys, computes the nine success rates from passed/design counts, and ranks the supplied fixture deterministically. A sweep awaiting required cost confirmation may complete the original preparation criteria but does not count as executed GPU validation; blocked setup or a failed launch is not a successful GPU result. Hardware selection and the completed work are documented in /workspace/output/execution_mode.json and the final answer, with the saved manifest and relevant artifact contents read back into tool output.",
      "assertions": [
        "The agent saved actual bounded hardware-probe evidence and /workspace/output/execution_mode.json with selected_mode, task_status, gpu_execution, reason, and evidence paths, then read back the saved manifest and probe result in tool output. The mode agrees with the observed allocation; a failed probe or gpu.available=false alone does not establish GPU absence. The manifest alone is not evidence that a GPU run occurred.",
        "GPU presence selects the original GPU branch, and confirmed GPU absence selects the CPU fallback. Missing dependencies/weights/target inputs, insufficient GPU memory, broken probes/drivers, or GPU execution failures are reported as blocked/failed rather than a CPU success. Undetermined hardware is blocked. No installation/download loop is used to hide these blockers.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent performed a preflight check to verify GPU availability and environment readiness. CPU, with confirmed GPU absence: The sweeper uses generation.model.bb_ca.simulation_step_params.sc_scale_noise and generation.search.best_of_n.replicas with the requested values and identifies best-of-n as the intended algorithm. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent proposed a sweeper YAML with axes for temperature [0.5, 1.0, 1.5] and search_replicas [2, 4, 8]. CPU, with confirmed GPU absence: The agent computes each success rate from n_passed/n_designs and writes all nine rows to /workspace/output/sweep_summary.csv. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent calculated and displayed the GPU-hour estimate before proceeding. CPU, with confirmed GPU absence: The agent sorts success rate descending with mean_i_pae ascending as the tie-breaker and identifies cfg_1_8 (temperature 1.0, replicas 8) as the best fixture row with a 0.8 success rate. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent described the output format including sweep_summary.csv with success rate ranking. CPU, with confirmed GPU absence: The final answer labels the result as analysis of synthetic fixture data, not measured performance on the biological target. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "The final answer and execution_mode.json distinguish a completed CPU fallback, a real GPU execution, a blocked/failed task, and a sweep awaiting confirmation. Synthetic CSV data is explicitly labeled. CPU completion is not presented as validation of GPU inference or design quality, and no secrets or fabricated scientific results are reported."
      ],
      "expected_skill": "complexa-sweep",
      "expected_script": null,
      "files": [
        "files/project",
        "files/sweep_results.csv"
      ]
    },
    {
      "id": "complexa-sweep-003",
      "prompt": "First inspect the actual resources allocated to this task; do not assume the host is CPU-only. Use the skill preflight if available, or equivalent bounded local probes in the baseline. Check GPU visibility with nvidia-smi and, if necessary, device allocation or an already installed CUDA runtime. A failed nvidia-smi call or preflight gpu.available=false alone does not prove GPU absence: distinguish a CPU allocation from a broken GPU probe using the available device/allocation evidence. Do not install a framework just to probe hardware. Save the probe output under /workspace/output.\n\nChoose exactly one branch:\n- GPU available: carry out the original request below on the prepared Complexa installation. Check the required runtime, GPU memory, checkpoints, refolder/tool dependencies, and target inputs first. /workspace/input/project contains partial configuration review fixtures, not a runnable installation. If GPU hardware is present but prerequisites are missing, the driver/probe fails, or execution fails/times out, report the exact blocker or failure; do not relabel the environment as CPU-only or substitute the CPU fallback for a successful GPU result. Ask for genuinely missing target/motif inputs instead of inventing them. Do not spend the trial cloning repositories, installing the ML stack, compiling extensions, or downloading weights; the GPU runner must be provisioned before this task.\n- No GPU available: complete only the CPU fallback below, using the supplied files and any installed skill references. Do not run inference, submit GPU jobs, install packages, clone repositories, or download model weights. Synthetic fixture values are test data, never measured design results.\n- Hardware availability cannot be determined: report the probe failure as a blocker, without claiming either a CPU completion or successful GPU execution.\n\nWrite /workspace/output/execution_mode.json containing selected_mode (gpu, cpu, or undetermined), task_status (completed, blocked, failed, or awaiting_confirmation), gpu_execution (completed, not_run, or failed), reason, and evidence (paths to the actual probe logs and, when applicable, execution logs/results). Keep the selected mode fixed if a GPU run fails. Also state the selected mode, actual work completed, output paths, and remaining blockers in the final answer. GPU success requires real completed-run evidence. CPU completion validates the fallback only and leaves the GPU test unexercised. Never fabricate results or expose secrets. For the GPU branch, retain the original sweep workflow, including cost estimation and any required launch confirmation. Execute once launch is authorized. If confirmation is still required, finish the concrete sweep setup, report awaiting_confirmation and gpu_execution=not_run, and do not claim a measured GPU result. Before the final answer, read back /workspace/output/execution_mode.json and show it together with the hardware-probe result in tool output. Reopen the written task artifacts and show their relevant contents or computed results; for large GPU outputs, show a bounded summary of actual run logs, counts, and metrics with their file paths.\n\nOriginal request (GPU branch):\nWe're preparing for a paper submission and need to ablate the reward weights in our Complexa binder pipeline. Specifically, we want a Pareto analysis of binder quality (iPAE) versus wall-clock time across different beam widths and reward scaling factors. Can you set this up for targets 02_PDL1 and 22_DerF21?\n\nCPU fallback (only when no GPU is available):\nUse the source config snippets in /workspace/input/project to look up parameter keys. Set up an offline Complexa reward-weight ablation for 02_PDL1 and 22_DerF21: beam widths [4, 8] and AF2 i_pae reward weights [-0.5, -1.0, -2.0]. Write a sweeper and the complete target/config grid. Separately analyze /workspace/input/pareto_results.csv, a synthetic fixture: write the Pareto-optimal config IDs for minimizing wall_clock_min and mean_i_pae, keeping both copies of equal nondominated points. Budget 30 GPU-minutes per target/config and leave launch pending. Write the requested fallback artifacts under /workspace/output.",
      "expected_output": "Hardware-dependent outcome. GPU branch: The agent used complexa-sweep to design a multi-target Pareto sweep over beam_width and reward scaling axes, computed the total run count across both targets, flagged the potentially large GPU cost, and outlined how the Pareto frontier would be extracted from the sweep_summary.csv. CPU branch, only when no GPU is available: The agent prepares six configurations per target (12 runs, 6 estimated GPU-hours) and correctly computes a two-objective Pareto frontier from the supplied fixture. A sweep awaiting required cost confirmation may complete the original preparation criteria but does not count as executed GPU validation; blocked setup or a failed launch is not a successful GPU result. Hardware selection and the completed work are documented in /workspace/output/execution_mode.json and the final answer, with the saved manifest and relevant artifact contents read back into tool output.",
      "assertions": [
        "The agent saved actual bounded hardware-probe evidence and /workspace/output/execution_mode.json with selected_mode, task_status, gpu_execution, reason, and evidence paths, then read back the saved manifest and probe result in tool output. The mode agrees with the observed allocation; a failed probe or gpu.available=false alone does not establish GPU absence. The manifest alone is not evidence that a GPU run occurred.",
        "GPU presence selects the original GPU branch, and confirmed GPU absence selects the CPU fallback. Missing dependencies/weights/target inputs, insufficient GPU memory, broken probes/drivers, or GPU execution failures are reported as blocked/failed rather than a CPU success. Undetermined hardware is blocked. No installation/download loop is used to hide these blockers.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent ran preflight and assessed the total cost as n_configs \u00d7 2 targets, presenting the GPU-hour estimate with a confirmation gate. CPU, with confirmed GPU absence: The sweeper uses generation.search.beam_search.beam_width and generation.reward_model.reward_models.af2folding.reward_weights.i_pae with the requested values. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent authored a sweeper YAML with axes for beam_width and reward weight scaling factors. CPU, with confirmed GPU absence: The target/config grid contains all 12 runs across the two targets and the budget is 6 GPU-hours at the supplied 30-minute estimate. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent explained that the Pareto frontier (wall-clock vs success/iPAE) would be identified from the aggregated results. CPU, with confirmed GPU absence: The agent writes a Pareto result containing exactly p0, p1, p3, and p5; p2 and p4 are dominated, and equal nondominated points p1 and p5 are both retained. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent asked the user to confirm the sweep size before launching, given the multi-target multiplication. CPU, with confirmed GPU absence: The agent explains the frontier minimizes both wall_clock_min and mean_i_pae and labels the CSV as synthetic data. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "GPU: pass only if the original sweep preparation is complete and launch prerequisites are ready (cost confirmation may remain pending), or an authorized sweep actually completed, AND the following criterion is met: The agent reports the sweep artifacts and required cost confirmation, or the actual outputs of an authorized completed sweep. CPU, with confirmed GPU absence: The agent leaves GPU launch pending, records the cost confirmation needed, and reports the output paths. Grade only the evidenced branch; GPU blockers/failures or undetermined hardware do not satisfy this assertion.",
        "The final answer and execution_mode.json distinguish a completed CPU fallback, a real GPU execution, a blocked/failed task, and a sweep awaiting confirmation. Synthetic CSV data is explicitly labeled. CPU completion is not presented as validation of GPU inference or design quality, and no secrets or fabricated scientific results are reported."
      ],
      "expected_skill": "complexa-sweep",
      "expected_script": null,
      "files": [
        "files/project",
        "files/pareto_results.csv"
      ]
    },
    {
      "id": "complexa-sweep-004",
      "prompt": "Show a SLURM submission template for a single existing Complexa job using configs/inference_configs/inf_pdl1.yaml, 4 GPUs and a 24-hour limit. Do not submit it or create a parameter sweep; use placeholders for site-specific account and partition settings.",
      "expected_output": "The agent provides a single-job SLURM template with the requested resources, no Cartesian expansion, and no dependency on a nonexistent complexa-slurm skill.",
      "assertions": [
        "The answer provides a single-job submission template with four GPUs and a 24-hour limit.",
        "The template references the provided config and marks unknown account/partition settings as placeholders.",
        "The agent does not create a sweeper or invoke complexa-sweep, does not submit a job, and does not claim that complexa-slurm is installed.",
        "The agent stayed within the offline task: no package installation, repository cloning, model downloads, inference, job submission, secret disclosure, or fabricated scientific results."
      ],
      "expected_skill": null,
      "expected_script": null,
      "files": []
    }
  ]
}
